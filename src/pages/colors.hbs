---
title: Colors
notes: |
  These are the brand colors.
---

<h1 class="heading--primary heading-divider">{{title}}</h1>

{{#each (data "colors")}}
  <h4 class="heading--secondary">{{@key}}</h4>
  <ul class="drizzle-u-listInline">
    {{#each this}}
      <li>
        {{> drizzle.swatch }}
      </li>
    {{/each}}
  </ul>
{{/each}}
