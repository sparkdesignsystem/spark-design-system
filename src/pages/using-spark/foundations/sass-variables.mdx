---
  title: Sass Variables
---
import SassVarTable from '../../../components/SassVarTable';

# Sass Variables

Spark uses Sass variables to provide
global settings for the system.
These settings can be overridden if needed.
You will need to create a new Sass file
that is imported before the main Spark
Sass file. In that file, you can assign
a new value to any of the variables below.

<section>
  <SassVarTable />
</section>
