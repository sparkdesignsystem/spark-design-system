description:
  These are the colors that have been chosen to represent the brand. These colors should be used in the correct
  proportion (see below) in all components of an application that require the use of color. The colors are set as
  variables in Spark and as document colors in the Design Toolkit.
sparkPackageCore: true
