<p class="sprk-u-mbm">
  See below for available customization options:
</p>

<table class="drizzle-c-Table drizzle-c-Table--wide sprk-u-mbm">
  <colgroup>
    <col class="sprk-u-Width-40">
    <col>
  </colgroup>

  <thead>
    <tr>
      <th>Input</th>
      <th>Type</th>
      <th>Description</th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td class="sprk-u-FontWeight--bold">
        splitAt
      </td>

      <td>
        string
      </td>

      <td>
        Can be 'tiny', 'small', 'medium', 'large', or 'huge'.
        Will cause the footer items to switch from a stacked
        layout to a side by side layout at the chosen
        'tiny', 'small', 'medium', 'large', or 'huge' breakpoint.
      </td>
    </tr>

    <tr>
      <td class="sprk-u-FontWeight--bold">
        badges
      </td>

      <td>
        array&#x0003C;object&#x0003E;
      </td>

      <td>
        This supplies the data for the badges in the footer.
        Each badge object in the array should have keys of alt
        (the image alt text), src (the image source),
        href, and analytics (the value for data-analytics).
        Each key should have its value of type string.
      </td>
    </tr>

    <tr>
      <td class="sprk-u-FontWeight--bold">
        navLinks
      </td>

      <td>
      array&#x0003C;object&#x0003E;
      </td>

      <td>
        This supplies the data for the navigation links in the footer.
        Each link object in the array should have keys of text (the display text), href,
        and analytics (the value for data-analytics).
        Each key should have its value of type string.
      </td>
    </tr>

    <tr>
      <td class="sprk-u-FontWeight--bold">
        additionalClassesBadges
      </td>

      <td>
        string
      </td>

      <td>
        Allows a string of classes to add, in
        addition to the spark classes. Intended
        for overrides.
      </td>
    </tr>

    <tr>
      <td class="sprk-u-FontWeight--bold">
        additionalClasses
      </td>

      <td>
        string
      </td>

      <td>
        Allows a string of classes to add, in
        addition to the spark classes. Intended
        for overrides.
      </td>
    </tr>
  </tbody>
</table>
