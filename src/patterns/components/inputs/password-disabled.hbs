---
name: Password Input - Disabled State
info:
  - Use *'s to mask the value by default.
  - Checking "Show Password" removes the mask and shows the actual password.
order: 20
invisible: true
---

{{#embed "patterns.components.inputs.password"
         idattr="password-disabled"
         disabled=true}}{{/embed}}
