---
name: Monetary Input
description:
  This input should be used to collect a monetary amount.
information:
  - On blur, format the input to include commas and a decimal followed by two zeros (ex. 1,000.00).
restrictions:
  - Do not use placeholder text.
order: 18
---

<div class="sprk-b-InputContainer" data-sprk-input="monetary">
  <div class="sprk-b-TextInput--monetary">
    <input class="sprk-b-TextInput {{#if class}}{{class}} {{/if}} sprk-u-Width-100 sprk-u-plm"
           pattern="(^\$?(\d+|\d{1,3}(,\d{3})*)(\.\d+)?$)|^$"
           id="{{#if idattr}}{{idattr}}{{else}}monetary-normal{{/if}}"
           type="tel"
           aria-describedby="{{#if idattr}}{{idattr}}{{else}}monetary-normal{{/if}}--error-container"
           {{#if invaliddata}} aria-invalid="true"{{/if}}
           {{#if disabled}} disabled{{/if}}
           data-sprk-input="text">
    <div class="sprk-b-InputContainer__input-border"></div>
    <label for="{{#if idattr}}{{idattr}}{{else}}monetary-normal{{/if}}" class="sprk-b-Label sprk-u-plm">Payment</label>
  </div>
  <div class="sprk-b-ErrorText"
       id="{{#if idattr}}{{idattr}}{{else}}monetary-normal{{/if}}--error-container">
    {{#if errorMessage}}
      <svg class="sprk-c-Icon sprk-c-Icon--m sprk-b-ErrorIcon" viewBox="0 0 100 100">
        <use xlink:href="#exclamation" />
      </svg>
      {{errorMessage}}{{/if}}</div>
</div>
