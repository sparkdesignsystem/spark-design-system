---
name: Textarea
description:
  This input should be used to collect
  something that cannot be captured in
  a single line input field. Text should
  wrap within the input field as it is
  populated with text.
order: 9
---

<div class="sprk-b-InputContainer">
  <label for="{{#if idattr}}{{idattr}}{{else}}textarea-normal{{/if}}" class="sprk-b-Label">Description</label>
  <textarea class="sprk-b-TextInput  {{#if class}}{{class}} {{/if}}sprk-u-Width-100"
            id="{{#if idattr}}{{idattr}}{{else}}textarea-normal{{/if}}"
            aria-describedby="{{#if idattr}}{{idattr}}{{else}}textarea-normal{{/if}}--error-container"
            {{#if disabled}} disabled{{/if}}
            {{#if invaliddata}} aria-invalid="true"{{/if}} data-sprk-input="text"></textarea>
  <div class="sprk-b-InputContainer__input-border"></div>
  <div class="sprk-b-ErrorContainer" id="{{#if idattr}}{{idattr}}{{else}}textarea-normal{{/if}}--error-container">
    {{#if errorMessage}}
      <svg class="sprk-c-Icon sprk-c-Icon--m sprk-b-ErrorIcon" viewBox="0 0 100 100">
        <use xlink:href="#exclamation" />
      </svg>
      <div class="sprk-b-ErrorText">
        {{errorMessage}}
      </div>
    {{/if}}
  </div>
</div>
