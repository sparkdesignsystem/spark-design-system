name: Inputs
description: |
  Examples of form elements.
contents:
  Text Input: inputs.html#text-input
  Checkboxes: inputs.html#checkboxes
  Radio Buttons: inputs.html#radio-buttons
  Select Box: inputs.html#select-box
  Textarea Input: inputs.html#textarea
  SSN Input: inputs.html#ssn-input
  Search Input: inputs.html#search-input
  Inline Search Input: inputs.html#inline-search-input
  Money Input: inputs.html#monetary-input
  Password Input: inputs.html#password-input
  Helper Text: inputs.html#helper-text
  Phone Number: inputs.html#phone-number-input
  Date Input: inputs.html#date-input
  Date Picker: inputs.html#date-picker
information:
  - Special characters used in formatting (like the hyphens in the SSN field) are not automatically removed
    from the field value and may need to be manually removed depending on your implementation details.
  - You should use <a class="drizzle-b-Link" href="#helper-text">helper text</a> when a user needs further
    clarification beyond the field label.
  - In Vanilla JS applications, if an input is required only, meaning that no validation is needed aside from a value
    being supplied, you can add the
    <code class='drizzle-c-Command drizzle-c-Command--inline'>data-sprk-required-only</code> attribute. The values that
    are available are 'text', 'select', and 'tick'. Text is for any input that accepts text as a value, Select is only
    for select inputs, and Tick is for radio buttons or checkboxes.
restrictions:
  - You should always add the <code class='drizzle-c-Command drizzle-c-Command--inline'>novalidate</code> attribute
    to your form, since we handle form validation ourselves,
    and don't use the browser's implementation.
  - Make sure that each label's <code class='drizzle-c-Command drizzle-c-Command--inline'>for</code> attribute matches the
    <code class='drizzle-c-Command drizzle-c-Command--inline'>id</code> attribute of the corresponding input element.
  - Each input element's <code class='drizzle-c-Command drizzle-c-Command--inline'>id</code> should be unique on the page.
  - You need to make sure that the <code class='drizzle-c-Command drizzle-c-Command--inline'>id</code> referenced in
    <code class='drizzle-c-Command drizzle-c-Command--inline'>aria-describedby</code> matches the
    <code class='drizzle-c-Command drizzle-c-Command--inline'>id</code> on the error container.
  - You should use helper text, as linked below, when a user needs further clarification beyond the label.
  - Labels and error messages should be short and to the point.
  - Labels should not be longer than what will fit on one line at 320px.
  - The data-id property is provided as a hook for automated tools. If you have multiple
    instances of the same variant of a component on the same page, make sure each instance
    has a unique data-id property ("input-text-1", "input-text-2", "input-date-1", etc).
sparkPackageCore: true
hasAngularInfo: true
variableTable:
  $sprk-input-gray-medium:
    default: #5a5a5a
    description: The primary color used for inputs.
  $sprk-monetary-symbol:
    default: '$'
    description: The symbol used in the before pseudo-element for monetary inputs.
  $sprk-input-placeholder-color:
    default: $sprk-gray
    description: The color of the input placeholder text.
  $sprk-input-readonly-text-color:
    default: $sprk-gray
    description: The input text color of inputs with the readonly attribute.
  $sprk-fieldset-padding:
    default: 0
    description: The padding around fieldset elements.
  $sprk-text-input-border-radius:
    default: 0
    description: The border radius on text inputs.
  $sprk-text-input-border-color:
    default: $sprk-black
    description: The border color on text inputs.
  $sprk-text-input-border-width:
    default: 0
    description: The border width on text inputs.
  $sprk-input-container-margin-bottom:
    default: $sprk-space-m
    description: The bottom margin on input containers.
  $sprk-input-disabled-background-color:
    default: $sprk-gray
    description: The background color of disabled inputs.
  $sprk-input-disabled-border-color:
    default: $sprk-black
    description: The border color of disabled inputs.
  $sprk-input-disabled-text-color:
    default: $sprk-input-gray-medium
    description: The text color of disabled inputs.
  $sprk-helper-text-hidden:
    default: false
    description: Shows helper text after a field when that field is in focus. A 'true' value will not show helper text.
  $sprk-text-input-padding:
    default: 3px $sprk-space-s
    description: The padding around text inputs.
  $sprk-input-readonly-border-color:
    default: $sprk-black
    description: The border color of readonly inputs.
  $sprk-input-box-shadow:
    default: 0 1px 0 0 $sprk-black
    description: The box-shadow on inputs.
  $sprk-input-error-box-shadow:
    default: 0 3px 0 0  $sprk-yellow
    description: The box-shadow on inputs with an error.
  $sprk-error-icon-size:
    default: 1.25rem
    description: The height and width size of the icon that displays when inputs have an error.
  $sprk-input-disabled-box-shadow:
    default: 0 1px 0 0 $sprk-input-gray-medium
    description: The box-shadow on disabled inputs.
  $sprk-date-picker-max-width:
    default: 20rem
    description: The maximum width of the date picker.
  $sprk-date-picker-button-size:
    default: 2.25rem
    description: The height and width of the clickable "day" buttons in the date picker.
  $sprk-date-picker-column-adjustment-x:
    default: 0.25rem
    description: This value is added to the value of the $date-picker-button-size to get the maximum width of the days of the week header.
  $sprk-date-picker-box-shadow:
    default: 0 2px 5px $sprk-input-gray-medium
    description: The box shadow of the date picker.
  $sprk-date-picker-day-interact-color:
    default: $sprk-red
    description: The interaction color of the days in the date picker.
  $sprk-date-picker-font-family:
    default: $sprk-font-family-body-two
    description: The font family used by the date picker.
  $sprk-text-input-interact-color:
    default: $sprk-red
    description: The interaction color of text inputs.
  $sprk-error-text-color:
    default: $sprk-black
    description: The text color of error messages.
  $sprk-text-input-error-color:
    default:  $sprk-yellow
    description: The text input error color used for icons, borders and error state styles.
  $sprk-input-max-width:
    default: 27.5rem
    description: The maximum width of the input container.
  $sprk-input-icon-offset-x:
    default: 7px
    description: The x offset value of input icons.
  $sprk-input-icon-offset-y:
    default: 21px
    description: The y offset value of input icons.
  $sprk-input-text-icon-offset-x:
    default: 6px
    description: The x offset value of icons inside the inputs.
  $sprk-input-text-icon-offset-y:
    default: 21px
    description: The y offset value of icons inside the inputs.
  $sprk-input-animation-speed:
    default: 0.3s
    description: The animation speed of input animations.
  $sprk-select-arrow-offset-y:
    default: -23px
    description: The vertical offset of the select container icon.
  $sprk-select-arrow-offset-x:
    default: 8px
    description: The horizontal offset of the select container icon.
  $sprk-label-offset-y:
    default: -53px
    description: The vertical offset value of labels.
  $sprk-label-shrink-scale:
    default: 0.82
    description: The value to scale to the labels by.
  $sprk-label-padding:
    default: 0
    description: The padding applied to labels.
classTable:
  .sprk-b-Label--no-input:
    description: Apply when a label is used without an input.
  .sprk-b-Label--inline:
    description: Used for inline labels.
  .sprk-b-TextInput--has-value:
    description: Text inputs that have a value.
  .sprk-b-TextInput--focusin:
    description: Text inputs that are currently in focus.
  .sprk-b-Label--with-icon:
    description: Labels that have an icon in them.
  .sprk-b-Label--monetary:
    description: For labels on monetary inputs.
  .sprk-b-Select--error:
    description: Adds select error styles.
  .sprk-b-Select--focusin:
    description: Selects that are currently in focus.
  .sprk-b-Select--focusout:
    description: Selects that are currently not in focus.
  .sprk-b-TextInputIconContainer--monetary:
    description: Monetary text inputs with icons.
