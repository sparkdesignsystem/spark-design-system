---
name: Password Input
description:
  This input is used to for entering a password. The value is masked by default.
information:
  - Checking "Show Password" removes the mask and shows the entered value.
order: 20
hasAngularCode: true
hasAngularDocs: true
---

<div class="sprk-b-InputContainer" data-sprk-input="password">
  <input class="sprk-b-TextInput {{#if class}}{{class}} {{/if}}sprk-u-Width-100"
         id="{{#if idattr}}{{idattr}}{{else}}password-normal{{/if}}"
         type="password"
         aria-describedby="{{#if idattr}}{{idattr}}{{else}}password-normal{{/if}}--error-container"
         {{#if invaliddata}} aria-invalid="true"{{/if}}
         {{#if disabled}} disabled{{/if}} data-sprk-input="text">
  <label for="{{#if idattr}}{{idattr}}{{else}}password-normal{{/if}}" class="sprk-b-Label">Password</label>
  <div class="sprk-b-InputContainer__input-border"></div>
  <div class="sprk-b-SelectionContainer sprk-b-InputContainer__visibility-toggle">
    <input id="{{#if idattr}}{{idattr}}{{else}}password-normal{{/if}}-show-password" type="checkbox"
      {{#if disabled}} disabled{{/if}}>
    <label for="{{#if idattr}}{{idattr}}{{else}}password-normal{{/if}}-show-password"
           class="sprk-b-Label sprk-b-Label--inline">Show Password</label>
  </div>
  <div class="sprk-b-ErrorContainer" id="{{#if idattr}}{{idattr}}{{else}}password-normal{{/if}}--error-container">
    {{#if errorMessage}}
      <svg class="sprk-c-Icon sprk-c-Icon--m sprk-b-ErrorIcon" viewBox="0 0 100 100">
        <use xlink:href="#exclamation" />
      </svg>
      <div class="sprk-b-ErrorText">
        {{errorMessage}}
      </div>
    {{/if}}
  </div>
</div>
