---
name: Monetary Input
description:
  This input is used to collect a monetary amount in US dollars.
information:
  - If no decimal amount is entered, the input will be automatically formatted with a decimal and two zeros on blur (ex. 1,000.00).
order: 18
---

<div class="sprk-b-InputContainer" data-sprk-input="monetary">
  <label for="{{#if idattr}}{{idattr}}{{else}}monetary-normal{{/if}}" class="sprk-b-Label">Payment</label>
  <div class="sprk-b-TextInput--monetary">
    <input class="sprk-b-TextInput {{#if class}}{{class}} {{/if}} sprk-u-Width-100 sprk-u-plm"
           pattern="(^\$?(\d+|\d{1,3}(,\d{3})*)(\.\d+)?$)|^$"
           id="{{#if idattr}}{{idattr}}{{else}}monetary-normal{{/if}}"
           type="tel"
           aria-describedby="{{#if idattr}}{{idattr}}{{else}}monetary-normal{{/if}}--error-container"
           {{#if invaliddata}} aria-invalid="true"{{/if}}
           {{#if disabled}} disabled{{/if}}>
  </div>
  <div class="sprk-b-ErrorText"
        id="{{#if idattr}}{{idattr}}{{else}}monetary-normal{{/if}}--error-container">
        {{#if errorMessage}}{{errorMessage}}{{/if}}</div>
</div>
