---
name: Search Input
description:
  This input should be used when the
  client has the ability to search
  within their experience by a word
  or phrase to find a specific piece
  of content without the use of navigation.
order: 13
---

<div class="sprk-b-InputContainer">
  <label for="{{#if idattr}}{{idattr}}{{else}}search-normal{{/if}}" class="sprk-b-Label">Search</label>
  <input class="sprk-b-TextInput {{#if class}}{{class}} {{/if}}sprk-u-Width-100"
         id="{{#if idattr}}{{idattr}}{{else}}search-normal{{/if}}"
         type="search"
         role="search"
         aria-describedby="{{#if idattr}}{{idattr}}{{else}}search-normal{{/if}}--error-container"
         {{#if invaliddata}} aria-invalid="true"{{/if}}
         {{#if disabled}} disabled{{/if}}>
  <div class="sprk-b-ErrorText"
        id="{{#if idattr}}{{idattr}}{{else}}search-normal{{/if}}--error-container">{{#if errorMessage}}{{errorMessage}}{{/if}}</div>
</div>
