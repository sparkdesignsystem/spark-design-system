---
name: Select Box
description:
  This input type should be used when a
  user needs to select a single option
  from a list of two or more options.
information:
  - Every item in the select input should be a valid option. As a best practice, do not put
    "Select an option..." or similar as the first option in the dropdown. If you want to provide
    further instructions related to this input, use Helper Text.
order: 7
hasAngularCode: true
---

<div class="sprk-b-InputContainer">
  <select class="sprk-b-Select{{#if class}} {{class}}{{/if}}" id="{{#if idattr}}{{idattr}}{{else}}select-normal{{/if}}"
          data-id="{{#if idattr}}{{idattr}}{{else}}select-normal{{/if}}"
          aria-describedby="{{#if idattr}}{{idattr}}{{else}}select-normal{{/if}}--error-container"
          {{#if disabled}} disabled{{/if}} data-sprk-input="select">
    <option value="1">Option 1</option>
    <option value="2">Option 2</option>
    <option value="3">Option 3</option>
    <optgroup label="Grouped Options">
      <option value="g1">Grouped Option 1</option>
      <option value="g2">Grouped Option 2</option>
      <option value="g3">Grouped Option 3</option>
    </optgroup>
  </select>
  <div class="sprk-b-InputContainer__input-border"></div>
  <svg class="sprk-c-Icon sprk-c-Icon--current-color sprk-b-SelectContainer__icon" viewBox="0 0 64 64">
    <use xlink:href="#chevron-down" />
  </svg>
  <label for="{{#if idattr}}{{idattr}}{{else}}select-normal{{/if}}" class="sprk-b-Label">Select Box Label</label>
  <div class="sprk-b-ErrorContainer" id="{{#if idattr}}{{idattr}}{{else}}text-input-normal{{/if}}--error-container">
    {{#if errorMessage}}
      <svg class="sprk-c-Icon sprk-c-Icon--m sprk-b-ErrorIcon" viewBox="0 0 64 64">
        <use xlink:href="#exclamation-filled" />
      </svg>
      <div class="sprk-b-ErrorText">
        {{errorMessage}}
      </div>
    {{/if}}
  </div>
</div>
