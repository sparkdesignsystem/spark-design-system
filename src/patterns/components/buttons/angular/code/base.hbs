<sprk-button
  {{#if buttonType}} buttonType="{{buttonType}}"{{/if}}
  {{#if disabled}} [isDisabled]="true"{{/if}}
  {{#if spinner}} [spinner]="true"{{/if}}
  {{#if additionalClasses}} additionalClasses="{{additionalClasses}}"{{/if}}>Hello, World.</sprk-button>
