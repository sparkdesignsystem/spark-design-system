---
name: Primary
description:
  Primary Buttons should only have a single action for
  the user to take and in most cases there should only be one Primary Button on any given page.
order: 1
hasAngularCode: true
hasAngularCodeInfo: true
---

<button class="sprk-c-Button{{#if class}} {{class}}{{/if}}{{#if disabled}} sprk-is-Disabled{{/if}}" {{#if button}} type="button"{{/if}} data-id="button-1" {{#if disabled}} disabled{{/if}}>
  Button
</button>
