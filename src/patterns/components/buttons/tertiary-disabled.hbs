---
name: Disabled Tertiary
description:
  Use a Disabled Button when an action is unavailable to the user.
  The Disabled styles can be applied to all Spark buttons. It's important to
  add the `disabled` attribute in addition to the `sprk-is-Disabled` class to indicate
  that the button should not receive focus.
order: 6
hasAngularCode: false
---

{{#embed "patterns.components.buttons.base" class="sprk-c-Button--tertiary" disabled=true}}{{/embed}}
