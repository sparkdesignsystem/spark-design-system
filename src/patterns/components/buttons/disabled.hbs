---
name: Disabled Button
description: |
  Add the `is-Disabled` class when an action is unavailable to the user.
  The disabled styles can be applied to all Spark buttons. It's important to
  add the `disabled` attribute in addition to the `is-Disabled` class to indicate
  that the button should not receive focus.
order: 5
---

{{#embed "patterns.components.buttons.base" disabled=true}}{{/embed}}
