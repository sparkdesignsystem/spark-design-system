<SprkButton
  {{#if disabled}} disabled{{/if}}
  {{#if loading}} loading{{/if}}
  {{#if variant}} variant="{{variant}}"{{/if}}
  {{#if idString}} idString="{{idString}}"{{/if}}
  {{#if classes}} additionalClasses="{{classes}}"{{/if}}
  >Button</SprkButton>
