import { SprkSpinnerDirective } from './sprk-spinner.directive';

describe('SprkSpinnerDirective', () => {
  it('should create an instance', () => {
    const directive = new SprkSpinnerDirective();
    expect(directive).toBeTruthy();
  });
});
